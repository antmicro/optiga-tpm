(version 1)


(rule "spi_inner"
	(layer inner)
	(condition "(A.NetClass == '50Ohm-se')")
	(constraint track_width (opt 0.18mm))
)

(rule "LTPI DiffPair In2"
    (condition "(A.NetClass == '100Ohm-diff')")
    (constraint track_width (opt 0.125mm))
    (constraint diff_pair_gap (opt 0.3mm))
)
